/*
 * ListBook Messages
 *
 * This contains all the text for the ListBook container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ListBook';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListBook container!',
  },
});
